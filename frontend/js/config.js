/**
 * config.js — Runtime API URL configuration.
 *
 * LOCAL DEV: leave this file unchanged. api.js falls back to localhost:4000.
 *
 * PRODUCTION (Vercel): set this to your Railway backend URL:
 *   window.NUSTRADE_API_URL = 'https://your-app.up.railway.app/api/v1';
 *
 * Load this BEFORE api.js in every HTML page.
 */

// Uncomment and set when deploying:
// window.NUSTRADE_API_URL = 'https://nustrade-backend.up.railway.app/api/v1';
